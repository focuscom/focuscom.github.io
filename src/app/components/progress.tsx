import Container from "@/components/container";

export default function Progress() {
  return (
    <section>
      <Container>
        <div className="p-6">
          <h2 className="mb-4">Effortless progress sharing</h2>
          <p className="mb-4">
            Every step of your journey is documented as you go, so when
            stakeholders need updates, there is no need to backtrack. Progress
            is always ready to share, keeping everyone aligned without extra
            effort.
          </p>
        </div>
      </Container>
    </section>
  );
}
